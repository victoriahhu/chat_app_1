import 'package:flutter/material.dart';
import '../widgets/all_widgets.dart';

class MyOrdersPage extends StatelessWidget with NavigationStates{
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text("My orders"),);
  }
}
